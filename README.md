# NPI-API
A NodeJS program that calls the NPPES NPI Registry API using search fields provided by the users
and prints out the information on physicians, if some are found. 

# To Run
To run this program, type "node index.js".
